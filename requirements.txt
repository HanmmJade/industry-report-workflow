# 行业月报自动化工作流 - 依赖包
# Python 3.7+ required

# 核心依赖
openpyxl>=3.0.0        # Excel文件生成
requests>=2.25.0       # HTTP请求（用于URL验证）

# 测试依赖
pytest>=7.0.0          # 测试框架
pytest-cov>=4.0.0     # 代码覆盖率

# 可选依赖
# dataclasses 已内置于 Python 3.7+
